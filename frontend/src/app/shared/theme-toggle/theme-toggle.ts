import { Component, ChangeDetectionStrategy } from '@angular/core';

/**
 * ThemeToggle — 已停用。
 * 網站目前僅使用深色模式，不再提供切換功能。
 * 保留此元件以避免破壞現有參照。
 */
@Component({
  selector: 'app-theme-toggle',
  standalone: true,
  template: ``,
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class ThemeToggleComponent {}
