# Koopa 配置範例
# 將此文件複製到 ~/.koopa/config.yaml 並修改

# AI 模型配置
model_name: "gemini-2.5-flash"
temperature: 0.7
max_tokens: 2048

# 資料庫路徑（可選，預設為 ~/.koopa/koopa.db）
# database_path: "/path/to/koopa.db"

# API Key（建議使用環境變數）
# gemini_api_key: "your-api-key-here"
